# Telegram Setup

- **Bot Token:** Configured ✅
- **Pairing Code:** `H2XHHYCK`
- **DM Policy:** pairing (requires pairing code to start DMs)
- **Group Policy:** allowlist (need to explicitly allow groups)

## How to use:
1. Find your bot on Telegram using the bot token
2. Send `/start H2XHHYCK` to pair your account
3. Once paired, you can chat normally

## To add to groups:
Configure the group allowlist in the OpenClaw config under `channels.telegram.groups`
